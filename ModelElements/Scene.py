from ModelElements.PoligonalModel import PoligonalModel


class Scene:
    def __init__(self, models):
        self.id = id
        self.models = models[PoligonalModel]
        self.flashes = []

    def method_1(self, type):
        for scene in self.scenes:
            if scene.id == id:
                return type
        return None

    def method_2(self, type1, type2):
        for scene in self.scenes:
            if scene.id == id:
                return type1, type2
        return None
